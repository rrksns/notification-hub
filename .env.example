# Alertmanager values. Keep SMTP password content in a local Secret file.
ALERTMANAGER_WEBHOOK_URL=
SMTP_SMARTHOST=
SMTP_FROM=
SMTP_AUTH_USERNAME=
SMTP_AUTH_PASSWORD_SOURCE=./.secrets/smtp-password
SMTP_AUTH_PASSWORD_FILE=/run/secrets/smtp-password
ALERT_EMAIL_TO=
